# Scaffold generated by Meno's JSON → meno-astro converter.
# Cloudflare Workers deploys: keep the adapter's worker bundle out of the
# static-asset upload (per Cloudflare's Astro framework guide). Inert on
# Netlify and plain static builds.
_worker.js
_routes.json
