---
import { resolveProps, style } from 'meno-astro';

const __props = resolveProps(Astro, {});
const { class: className } = __props;
---
<div class={style({
    base: {
      display: "flex",
      flexDirection: "column",
      marginBottom: "12px",
      gap: "8px"
    },
    tablet: {},
    mobile: {}
  }, __props, { root: true })}><slot /></div>
