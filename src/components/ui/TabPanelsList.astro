---
import { cx, resolveProps } from 'meno-astro';

const __props = resolveProps(Astro, { fade: { type: "string", default: "" } });
const { fade, class: className } = __props;
---
<div class={cx("w-full", className)} fade={fade || undefined}><slot /></div>
