// Scaffold generated by Meno's JSON → meno-astro converter.
// Cloudflare Workers static-assets deploy: `astro build` then `wrangler deploy`.
// Presence of this file makes Cloudflare Workers Builds skip its automatic
// framework setup (`astro add cloudflare`), which a Meno project doesn't need —
// the site is fully prerendered, so it deploys as pure static assets.
{
  "name": "template-marcus-astro-astro-tmp-1781344415258-tlfrij",
  "compatibility_date": "2026-06-13",
  "assets": {
    "directory": "./dist",
    "not_found_handling": "404-page"
  }
}
